'use client';
import Link from 'next/link'; import {Menu,X,ArrowUpRight} from 'lucide-react'; import {useEffect,useState} from 'react';
const links=[['Work','/work'],['Services','/services'],['Process','/process'],['About','/about']];
export function Header(){const [open,setOpen]=useState(false);useEffect(()=>{const onKey=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[]);return <header className="fixed top-0 left-0 right-0 z-40"><div className="container mt-4"><div className="glass rounded-2xl px-5 h-16 flex items-center justify-between shadow-lg shadow-black/10"><Link href="/" className="font-bold tracking-tight text-lg" aria-label="RapidlyOn home">Rapidly<span className="text-violet-400">On</span></Link><nav className="hidden md:flex items-center gap-7 text-sm text-zinc-300" aria-label="Primary navigation">{links.map(([x,h])=><Link key={h} href={h} className="hover:text-white transition">{x}</Link>)}</nav><Link href="/start-a-project" className="hidden md:inline-flex items-center gap-2 rounded-xl bg-white text-black px-4 py-2 text-sm font-semibold hover:bg-zinc-200 transition">Start a Project <ArrowUpRight size={15}/></Link><button className="md:hidden p-2" onClick={()=>setOpen(v=>!v)} aria-label={open?'Close menu':'Open menu'} aria-expanded={open}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="md:hidden mt-2 glass rounded-2xl p-4 flex flex-col gap-2 shadow-xl">{links.map(([x,h])=><Link onClick={()=>setOpen(false)} className="p-3 rounded-xl hover:bg-white/5" key={h} href={h}>{x}</Link>)}<Link onClick={()=>setOpen(false)} className="p-3 rounded-xl bg-white text-black font-semibold" href="/start-a-project">Start a Project</Link></div>}</div></header>}
